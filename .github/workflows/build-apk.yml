name: Build APK (Expo)

on:
  # Manuelles Starten im Actions-Tab
  workflow_dispatch:
  # Optional: automatisch bei Push auf main
  push:
    branches: [ main ]

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Repository herunterladen
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Node.js einrichten (Expo nutzt aktuell Node 20)
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Abhängigkeiten installieren
      - name: Install dependencies
        run: npm ci

      # 4️⃣ EAS CLI installieren (zum Bauen der APK)
      - name: Install EAS CLI
        run: npm install -g eas-cli

<<<<<<< HEAD
      # 5️⃣ Mit Expo authentifizieren (neue Methode)
=======
>>>>>>> b4b4d0d2ef1c4fe8e6039ec48600fb31898dc105
      - name: Authenticate with Expo
        run: eas session:authenticate --token ${{ secrets.EXPO_TOKEN }}

      # 6️⃣ APK für Android bauen (laut deinem eas.json)
      - name: Build APK
        run: eas build --platform android --profile production --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      # 7️⃣ Das Ergebnis als Download-Artefakt bereitstellen
      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: weihnachtsbingo-apk
          path: dist/*.apk
